---
type: skill
status: active
tags: [architecture, master-spec, liquid-glass, ai-first, erp, ecommerce]
security_level: tier-1
version: 3.0
---

# 🌐 MASTER SKILL: LIQUID AI-FIRST COMMERCE PLATFORM

[cite_start]This document serves as the absolute Single Source of Truth (SSOT) and Master Architecture Skill for the entire application environment[cite: 1020, 1503, 1707]. [cite_start]It governs both the consumer-facing shopping layer and the high-end LIQUID ERP administration dashboard[cite: 1015, 1016, 1954, 1955].

---

## 🛑 1. GLOBAL GOVERNANCE & TOKEN DIET ARCHITECTURE

### 📉 AUTOMATIC TOKEN OPTIMIZATION RULES
[cite_start]To enforce extreme token efficiency and allow the runtime engine to fully utilize fast, cost-effective models (e.g., Gemini 1.5 Flash) for 80% of daily tasks, the AI Agent must strictly obey these directory barriers[cite: 1030, 1582, 1591]:
* [cite_start]**Absolute Search Bans:** Never perform recursive full-text workspace searches or lookups across the following compiled/dependency environments: `.next/`, `node_modules/`, `.obsidian/`[cite: 1300, 1362, 1813].
* [cite_start]**Modular Lookup:** Always reference `AI_Index.md` first to fetch explicit path blueprints instead of guessing file trees[cite: 1301, 1363, 1601].
* [cite_start]**Context Isolation:** When instructed to work on a specific client view or action pipeline, ingest only the specified modules to maintain a clean context window[cite: 1585, 1804].

### ⚡ BACKEND ARCHITECTURAL RIGIDITY
* [cite_start]**Zero REST APIs:** No raw API endpoints or `route.ts` handlers are permitted under feature matrices[cite: 1011, 1113, 1961]. [cite_start]Communication relies 100% on **Next.js 15 Server Actions**[cite: 1011, 1113].
* [cite_start]**Intent over Inputs Layout:** State parameters and data models must be exposed to support both traditional UI mouse/touch gestures and dynamic natural language JSON strings generated by the AI Chatbot[cite: 1009, 1326, 1354].
* [cite_start]**Serverless Safety:** Database operations using Mongoose must handle pooling explicitly via cached connection wrappers to eliminate runtime leakages in serverless edge environments[cite: 1041, 1137].
* [cite_start]**Strict Model Safety:** Export Mongoose models using the `(mongoose.models.ModelName || mongoose.model(...))` design to explicitly override Next.js Hot Module Replacement (HMR) re-compilation failures[cite: 1694].

---

## 🎨 2. LIQUID GLASS 3.0 & NATIVE RTL (HEBREW) UX MATRIX

[cite_start]Every view, component, layout drawer, and bento block must honor the high-end luxury styling rules of the design token system[cite: 1012, 1209].

### 💎 VISUAL SPECIFICATIONS
* [cite_start]**Aesthetic Palette:** Deep luxurious slate backgrounds (`#05070B` / `bg-slate-950`), crystal clear layer definitions using `backdrop-blur-md` and `bg-slate-900/40`, paired with semi-transparent borders (`border-white/10`)[cite: 1012, 1115, 1167, 1747].
* [cite_start]**Pulsing Accents:** Flash neon gold (`#FFB800`) and cyber teal (`#00F0FF`) parameters for premium feedback cues, priority indicators, and timers[cite: 1188, 1747, 1958].
* [cite_start]**Micro-Animations:** Use React `useOptimistic` hook configurations or Framer Motion properties to guarantee immediate 0ms visual reactions on item adjustments or mutations, processing heavy network validation silently behind the scenes[cite: 1012, 1069, 1172, 1293].

### 🌍 RTL FLOW CONTROLS (HEBREW LOCALIZATION)
* **Tailwind Logical Properties:** Hardcoded left/right CSS elements are strictly forbidden. [cite_start]Utilize `ps-`, `pe-`, `space-x-reverse`, `flex-row-reverse`, and `rtl:translate-x-0` exclusively to naturally match reading orientation[cite: 1115, 1167, 1960, 1963].
* [cite_start]**Typography Layouts:** Render text arrays using premium Hebrew families (`Assistant`, `Rubik`, or `Heebo`) bound strictly to `leading-relaxed` parameters to prevent vertical line clippings or character distortion[cite: 1115, 1289, 1360, 1959].
* [cite_start]**Icon Realignment:** Mirror directional interface elements (such as arrows, chevrons, and sliding closing buttons) to respect right-to-left layout constraints[cite: 1115, 1290, 1361].

---

## 🧠 3. CONSUMER-FACING CORE SKILLS MATRIX

### 💬 SKILL 01: Dynamic AI Concierge
* [cite_start]**Role:** A floating, context-aware conversational commerce bridge[cite: 1015, 1176, 1618].
* [cite_start]**Operation:** Parses free-text Hebrew phrases from the input block, dynamically maps the raw intent into a structured semantic payload, validates it via Zod, and issues direct triggers to the `cartActions.ts` file[cite: 1015, 1114, 1182, 1353].

### 🛒 SKILL 02: Liquid Cart Layer
* [cite_start]**Role:** Zero-latency slide-in shopping bag panel (`@/src/components/cart/CartDrawer.tsx`)[cite: 1016, 1157, 1364].
* [cite_start]**Operation:** Slides out smoothly from the right viewport limit[cite: 1016, 1167, 1372]. [cite_start]Implements a `useOptimistic` wrapper around item lists to display deletion or counter modifications in **0ms**, reconciling the final state with MongoDB mutations under the hood[cite: 1069, 1166, 1172]. [cite_start]Price fields must follow standard RTL placement formatting (e.g., `₪180` or `180 ₪`)[cite: 1375].

### 🏷️ SKILL 03: Contextual Price Anchor
* [cite_start]**Role:** Real-time dynamic business rule and financial conversion processor[cite: 1017, 1119].
* [cite_start]**Operation:** Evaluates active user context parameters, tracks loyalty status levels, and modifies baseline product prices dynamically across layout blocks while updating line-item tallies in real-time[cite: 1017, 1311].

### 💳 SKILL 04: Smart-Checkout Pipeline
* [cite_start]**Role:** Highly secure, transaction verification and execution gateway[cite: 1018, 1119, 1312].
* [cite_start]**Operation:** Coordinates payment requests across server vectors[cite: 1018]. [cite_start]Enforces total data integrity by sealing purchase objects inside strict Zod gates prior to processing state-machine mutations inside database collections[cite: 1018, 1312, 1356].

---

## 📊 4. ADVANCED LIQUID ERP ADMIN CONTROL MATRIX

[cite_start]The administration layer (`/admin/liquid-erp`) provides corporate management and ecosystem monitoring using zero-latency visual matrices[cite: 1954, 1955].

### 💎 DEPLOYED ERP PREMIUM FEATURES
1. [cite_start]**Real-Time Liquidity Stream:** A high-end glassmorphic bento block combining multi-workspace sales metrics, raw database transactional revenues, and immediate user shopping cart valuations using optimized MongoDB atomic aggregations[cite: 1954].
2. **Predictive AI Supply Chain:** A semantic operational model allowing administrators to handle inventory levels using natural commands. [cite_start]Translates conversational statements (e.g., "Predict stock adjustments") into programmatic restock delta chains[cite: 1954].
3. [cite_start]**Anomalous Intent Radar:** An active context firewall tracking user interaction sequences with consumer-facing chatbots to block code injection or exploit manipulation vectors[cite: 1954].
4. [cite_start]**Dynamic Tax & Currency Anchor:** A centralized layout toggle that instantly propagates global taxation shifts or currency base modifications across active calculations[cite: 1954].

### 🛡️ HERMETIC VALIDATION GATES (`src/lib/validations/erpAdmin.ts`)
```typescript
import { z } from "zod";

export const UpdateInventorySchema = z.object({
  productId: z.string().min(1, { message: "מזהה מוצר הוא שדה חובה" }),
  stockDelta: z.number().int().refine((val) => val !== 0, {
    message: "שינוי המלאי חייב להיות מספר שלם שונה מאפס",
  }),
  reason: z.string().min(5, { message: "נימוק השינוי חייב להכיל לפחות 5 תווים" }),
});

export const AdjustFinancialAnchorSchema = z.object({
  baseCurrency: z.enum(["ILS", "USD", "EUR"], { message: "מטבע בסיס לא נתמך במערכת" }),
  globalTaxRate: z.number().min(0).max(100, { message: "אחוז המס חייב להיות בין 0 ל-100" }),
  securityBypassToken: z.string().optional(),
});

export const FlagAnomalySchema = z.object({
  sessionId: z.string().min(1, { message: "מזהה סשן לא תקין" }),
  actionTaken: z.enum(["BLOCK", "MONITOR", "ALLOW"]),
});

export type UpdateInventoryPayload = z.infer<typeof UpdateInventorySchema>;
export type AdjustFinancialAnchorPayload = z.infer<typeof AdjustFinancialAnchorSchema>;
export type FlagAnomalyPayload = z.infer<typeof FlagAnomalySchema>;