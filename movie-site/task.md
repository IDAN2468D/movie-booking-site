# Active Sprint Ledger

## Sprint 62: External Standalone Placement for Quantum Payment Splitter
- `[ ]` Remove `PaymentSingularityMatrix` from narrow `components/booking/BookingSummarySidebar.tsx`.
- `[ ]` Place `PaymentSingularityMatrix` as a full-width standalone module in `app/(main)/checkout/page.tsx`.
- `[ ]` Place `PaymentSingularityMatrix` as a full-width 12-column module in `app/(main)/book/[movieId]/[branchId]/page.tsx`.
- `[ ]` Run `npx tsc --noEmit`.
- `[ ]` Run `npx vitest run`.
- `[ ]` Run `npm run build`.
